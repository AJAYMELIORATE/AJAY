#include<stdio.h>
#include<math.h>

int main()
{
    int number;
    scanf("%d",&number);
    printf("By doubling the number without using the arthematic operations we get : %d",number >> 1 );
    return 0;
}
